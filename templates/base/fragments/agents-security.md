- **NO Hardcoded Secrets:** Never commit API keys, tokens, or credentials to git
- **Input Validation:** Validate all inputs at system boundaries
- **Environment Variables:** All secrets via `.env.local` (git-ignored) or secret manager
- **Type Safety:** Use strict type checking where the language supports it

<!-- CUSTOMIZE: Add project-specific security requirements -->
