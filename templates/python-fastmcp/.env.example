# Copy this to .env.local and fill in your values.
# Shared non-secret config lives in .env.development (checked in).

# Server
PORT=8000
# <!-- CUSTOMIZE: PACKAGE_NAME -->_TRANSPORT=streamable-http   # streamable-http | stdio | sse
# <!-- CUSTOMIZE: PACKAGE_NAME -->_ENV=development             # development | staging | production

# API Keys (add as needed)
# SOME_API_KEY=your-key-here

# Override any .env.development value here
