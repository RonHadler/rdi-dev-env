# Shared non-secret development config (checked into version control).
# Secret values go in .env.local (gitignored).

PORT=8000
<!-- CUSTOMIZE: PACKAGE_NAME -->_TRANSPORT=streamable-http
<!-- CUSTOMIZE: PACKAGE_NAME -->_ENV=development
