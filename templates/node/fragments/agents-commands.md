```bash
# Development
npm run dev                 # Start dev server (tsx watch)

# Testing
npm test                    # vitest run
npm run test:watch          # vitest (watch mode)

# Code Quality
npm run lint                # eslint
npx tsc --noEmit            # type check

# Building
npm run build               # tsc compile
make clean                  # Remove dist/, cache, coverage
```
