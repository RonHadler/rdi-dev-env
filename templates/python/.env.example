# <!-- CUSTOMIZE: Project Name --> — Local Environment Overrides
# Copy to .env.local and fill in your values.
# .env.local is git-ignored; .env.development is shared (no secrets).

PORT=8000
<!-- CUSTOMIZE: PACKAGE_NAME -->_ENV=development
LOG_LEVEL=info
